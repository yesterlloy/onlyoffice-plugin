spring:
  # 数据源配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/onlyoffice?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      # 监控配置
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: admin
      web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*"

# 阿里云 OSS 配置
aliyun:
  oss:
    endpoint: oss-cn-guangzhou.aliyuncs.com
    bucket-name: template-editor-dev
    access-key-id: your-access-key-id
    access-key-secret: your-access-key-secret
    custom-domain:

# AI 模型配置
ai:
  local:
    base-url: http://49.232.215.235:11434/v1
    api-key:
    default-model: qwen3-coder-next:q8_0
    default-temperature: 0.3
    default-max-tokens: 1000
    timeout:
      connect: 10000
      read: 60000

logging:
  level:
    com.yl.template: DEBUG